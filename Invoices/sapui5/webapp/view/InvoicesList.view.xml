<mvc:View controllerName="coa.sapui5.controller.InvoicesList"
          xmlns="sap.m"
          xmlns:mvc="sap.ui.core.mvc">
    <List headerText="{i18n>invoiceListTitle}" 
          class="sapUiResponsiveMargin" 
          width="auto" 
          items="{invoices>/Invoices}">
        <items>
            <ObjectListItem title="{invoices>Quantity}"
                            number="{ parts : [ {path : 'invoices>ExtendedPrice'}, 
                                                {path : 'currency>/usd'}],
                                    type: 'sap.ui.model.type.Currency',
                                    formatOptions: {
                                        showMeasure : false
                                    }}"
                            numberUnit="{currency>/usd}"
                            numberState="{= ${invoices>ExtendedPrice} > 50 ? 'Success' : 'Error'}">
            <firstStatus>
                <ObjectStatus text="{ path: 'invoices>Status',
                                       formatter: '.formatter.invoiceStatus' }"></ObjectStatus>
            </firstStatus>
            </ObjectListItem>            
        </items>
    </List>
</mvc:View>  